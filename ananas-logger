<!DOCTYPE HTML>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="Keywords" content="blog"/>
    <meta name="Description" content="blog"/>
    <title>Simple</title>
    <link rel="shortcut icon" href="/static/favicon.png"/>
    <link rel="stylesheet" type="text/css" href="/main.css" />
</head>
<body>
<div class="main">
    <div class="header">
    	<ul id="pages">
            <li><a href="/">home</a></li>
            <li><a href="/#/tags">tags</a></li>
            <li><a href="/#/archive">archive</a></li>
    	</ul>
    </div>
	<div class="wrap-header">
	<h1>
    <a href="/" id="title"></a>
	</h1>
	</div>
<div id="md" style="display: none;">
<!-- markdown -->
//复制粘贴的，暂时没有用markdown
  本来打算写一下ananas::future的实现，考虑了一下还是算了。Future实现有太多C++11模板的语法细节，我主要也是在读懂folly的基础上，将它们浓缩到future/Helper.h和Try.h两个文件。另外Then的初级实现也改自folly。还有就是权衡轻重，我选择了在std::promise的基础上封装，并没有从头写，我认为重点是先关注功能和正确，在不影响程序重构难度的情况下，先不要考虑性能。有兴趣的可以自己看下ananas::future（个人觉得完全吃透folly::future性价比很低，也很容易掉进folly的大海迷失方向，而ananas::future在精简的基础上仍不失功能的完整性），提示下可以重点看看timeout的实现，内部非常健壮（保证timeout和then链式调用是互斥调用的且必有仅有其一发生，也保证了链式调用自身的原子性！）。 抛开future，今天就谈一个最常用的功能：日志。 ananas::logger是一个采用流式格式输出的高性能多线程日志库，在每写日志都调用flush的情况下，速度仍然达到300万行/秒以上。logger完全使用C++11标准库实现，天然跨平台。 


一 导语

     日志是后台开发中最最常用的功能，然而常常由于性能问题，我们不得不在生产环境关闭日志，使得现网问题难以排查。尽管通过一些远程日志可以查到一些蛛丝马迹，我们还是希望能在本地看到一些重要信息。如果是调试分布式程序呢？本地日志尤为重要。我们希望日志能够有足够高的性能，可以放心的在生产环境开启至少warning级别以上的日志，甚至负载不高的情况下开全量日志！ananas::logger在保证多线程写入安全、每次都flush的情况下，仍然提供了超高的性能。废话不说下面直奔实现去。

二 内部实现概要

   ananas::logger的工作原理，大概如图所示：



logger对象为每一个业务线程维护了一个日志buffer,这个buffer需要多大？就是你一条日志的最大大小，一般来说2KB足够了。buffer被业务线程和日志IO线程共同操作，如何保证线程安全呢？回答环形buffer的我觉得可以给0分。用了环形buffer少不了sleep+轮询，非常丑陋，在有些场合还会导致生产者阻塞。老老实实用条件变量和锁，不搞些乌七八糟的东东。

图看上去很简单对吧？实现上有少许细节需要考虑，比如如何确保在没有被kill -9的情况下不丢日志（据skwang大神说muduo也没有做到。。），确保日志对象的生命期有效（如果业务层在全局对象的析构函数中写日志。。）。

三 业务线程的写入实现

 先看一下日志的格式：

  

 2017-02-21[16:06:25.764][INF]:Hello, I am listen on 8402|139872813981632

 

可以看到，日志的前半部分，是时间戳 + 日志级别，它们的长度是固定的。冒号后面是业务填写的日志内容，变长。竖线后面是线程的id（之所以线程id放最后面，因为不同系统上长度可能不同）。

实际上，前面框架图我画的忽略了非常多的代码细节。一条日志先要经过格式化，参数的填充，时间戳、等级的填充，最终才可以放到buffer中交给IO线程处理。图上并没有反应出来。格式化的过程使用了一个中间缓冲区，我称为tmpBuffer：

static const size_t kMaxCharPerLog = 2048;
static thread_local char tmpBuffer_[kMaxCharPerLog];

（ 这里我简单设置为单条日志不超过2KB，实际可以改为参数控制，不是重点，先忽略）

可能有人会问为什么要定义成线程局部变量，直接用局部变量不好吗？答案是：为了优化。看下日志的时间戳格式化，一般来说，秒之前的部分变化极少，因此很有必要将格式化的结果缓存起来，让日志之间复用这段buffer。同样我也使用了一个线程局部的缓冲区缓存线程id的字符串，线程id是不变的，更适合缓存：static thread_local char tid_[16]。之所以static，是因为可以供所有的logger对象共享这些内容，假如一个业务线程操作多个logger对象的情况。最后说下，缓存时间的做法来自陈硕的blog，最初我有点无视这点性能提升，实测之后发现比我想象的要好，于是采用了。

当tmpBuffer中将日志格式化好之后，才放入buffer，也就是图上的buffer，在代码中定义是BufferInfo类型。BufferInfo是thread_local变量，这样每个业务线程有自己的日志BufferInfo，不会冲突。而且BufferInfo要保证线程互斥访问，因为IO线程要读它。ananas::logger采用了map来管理这些BufferInfo，使用thread_id作索引。并使用锁来保护，因为无论是map还是BufferInfo本身都不是线程安全的：

struct BufferInfo
{
       std::atomic<bool> inuse_{false};
       Buffer buffer_;
};

std::mutex mutex_;
std::map<std::thread::id, std::unique_ptr<BufferInfo> > buffers_;


 

业务线程这样来获取BufferInfo，详见代码注释：

    BufferInfo* info = nullptr;
    {   
        std::unique_lock<std::mutex> guard(mutex_);
        if (shutdown_)
        {   

            //不应该发生，除非是全局对象析构函数中写日志，此时日志IO线程已退出
            std::cout << tmpBuffer_;
            return;
        }   

        info = buffers_[std::this_thread::get_id()].get();
        if (!info) // 没找到自己的buffer就新建一个
            buffers_[std::this_thread::get_id()].reset(info = new BufferInfo());


        // inuse_标志使得此buffer在使用过程中不会被IO线程访问。
        assert (!info->inuse_);
        info->inuse_ = true;
    }   

    // 好啦，锁释放了，在使用锁的过程中没有任何耗时操作。

 在获取到BufferInfo之后，业务线程写入logger等级和内容长度信息，代码就不展示了。正常情况下写日志就结束了，但是，如果生产者过快，缓存的日志内容很多，就需要主动唤醒IO线程：

  if (info->buffer_.ReadableSize() > kFlushThreshold)
  { 

      // 日志内容太多，AddBusyLog实际上会唤醒IO线程写日志
      info->inuse_ = false;
      LogManager::Instance().AddBusyLog(this);
  }
  else
  {
       info->inuse_ = false;
  }

 写日志的过程到此就结束了，下面看看IO线程如何将日志写入到文件。

四 IO线程的日志落地实现

IO线程所做的事很简单，就是在BufferInfo集的条件变量上定时等待，默认设置的等待时间是5ms，我觉得3-10ms都是OK的。但如果日志过多，它会被业务线程提前唤醒，这是与sleep的本质不同。线程主体代码如下：

    bool run = true;
    while (run)
    {
        std::vector<Logger* > tmpBusy;

        {
            std::unique_lock<std::mutex> guard(mutex_);
            cond_.wait_for(guard, kFlushInterval); // 最多等待5ms
            if (!busyLogs_.empty())
            {

                //说明是被唤醒，有业务线程调用了AddBusyLog
                tmpBusy.assign(busyLogs_.begin(), busyLogs_.end());
                busyLogs_.clear();
            }

            if (shutdown_)
                run = false;
        }

        if (tmpBusy.empty())
        {

            // 说明是超时醒来
            std::unique_lock<std::mutex> guard(logsMutex_);
            for (auto& plog : logs_)
                tmpBusy.push_back(plog.get());
        }

        for (auto plog : tmpBusy)
        {  // 执行落地操作，Update函数具体见下面
            plog->Update();
        }
    }

 


单个logger对象的Update落地函数实现（代码中assert比较多，这不但验证了代码的健壮性，也增加了可读性），其中的sth_todo是为了告知IO线程，我还有数据需要落地，你不要急着退出。

bool  sth_todo = false;

{
    std::unique_lock<std::mutex> guard(mutex_);

    for (auto it(buffers_.begin()); it != buffers_.end(); )
   {   
        assert (it->second);

        if (it->second->inuse_)
        {   
             // if logs is still in producing, there will be some work to do.
             sth_todo = true;
            ++ it; 
        }   
        else
        {   
               tmpBufs.push_back(std::move(it->second));
               it = buffers_.erase(it);
        }   
   }   
}


// 执行落地操作
for (auto& pbuf : tmpBufs)
{ 
    auto nWritten = _Log(pbuf->buffer_.ReadAddr(), pbuf->buffer_.ReadableSize());
    assert (nWritten == pbuf->buffer_.ReadableSize());
} 
// 每次写了文件都要flush
file_.flush();

 还有一些关于流式输出的实现，我觉得都是C++语法运用的问题了，不是什么重点，大家参考下protobuf的日志的流式实现就行了。以及未开启日志级别时不评估计算参数，这是流式天然具备的，printf流的担心是没必要的。

 五 性能思考

   在公司的tlinux2.0系统开发机上和我的2013rMBP上做了测试，O2优化：

    1业务线程： 210万行/秒

    2业务线程：310万行/秒

    4业务线程：350万行/秒

    8业务线程：320万行/秒

性能比陈硕的muduo logger高出50%（他的书中声称是200万行/秒）。而我的实现原理更为简单，没有什么双缓冲、环形buffer这些东东。主要得益于对临界区的精简，没有在临界区做任何耗时的事情。所以没那么多玄学，不是代码优化的好，而是我没有做劣化的操作。

最后补充个测试细节，在两个平台上，使用mmap映射日志文件，性能都是300w左右；使用std::fstream，在linux上仍然是300w，但在mbp上使用flush性能降的厉害，因此最后我提供了mmap的支持。当然如果只在linux上跑使用标准库fstream就行了。

<!-- markdown end -->
</div>
<div class="entry" id="main">
<!-- content -->
<p>//复制粘贴的，暂时没有用markdown
  本来打算写一下ananas::future的实现，考虑了一下还是算了。Future实现有太多C++11模板的语法细节，我主要也是在读懂folly的基础上，将它们浓缩到future/Helper.h和Try.h两个文件。另外Then的初级实现也改自folly。还有就是权衡轻重，我选择了在std::promise的基础上封装，并没有从头写，我认为重点是先关注功能和正确，在不影响程序重构难度的情况下，先不要考虑性能。有兴趣的可以自己看下ananas::future（个人觉得完全吃透folly::future性价比很低，也很容易掉进folly的大海迷失方向，而ananas::future在精简的基础上仍不失功能的完整性），提示下可以重点看看timeout的实现，内部非常健壮（保证timeout和then链式调用是互斥调用的且必有仅有其一发生，也保证了链式调用自身的原子性！）。 抛开future，今天就谈一个最常用的功能：日志。 ananas::logger是一个采用流式格式输出的高性能多线程日志库，在每写日志都调用flush的情况下，速度仍然达到300万行/秒以上。logger完全使用C++11标准库实现，天然跨平台。 </p>

<p>一 导语</p>

<pre><code> 日志是后台开发中最最常用的功能，然而常常由于性能问题，我们不得不在生产环境关闭日志，使得现网问题难以排查。尽管通过一些远程日志可以查到一些蛛丝马迹，我们还是希望能在本地看到一些重要信息。如果是调试分布式程序呢？本地日志尤为重要。我们希望日志能够有足够高的性能，可以放心的在生产环境开启至少warning级别以上的日志，甚至负载不高的情况下开全量日志！ananas::logger在保证多线程写入安全、每次都flush的情况下，仍然提供了超高的性能。废话不说下面直奔实现去。
</code></pre>

<p>二 内部实现概要</p>

<p>ananas::logger的工作原理，大概如图所示：</p>

<p>logger对象为每一个业务线程维护了一个日志buffer,这个buffer需要多大？就是你一条日志的最大大小，一般来说2KB足够了。buffer被业务线程和日志IO线程共同操作，如何保证线程安全呢？回答环形buffer的我觉得可以给0分。用了环形buffer少不了sleep+轮询，非常丑陋，在有些场合还会导致生产者阻塞。老老实实用条件变量和锁，不搞些乌七八糟的东东。</p>

<p>图看上去很简单对吧？实现上有少许细节需要考虑，比如如何确保在没有被kill -9的情况下不丢日志（据skwang大神说muduo也没有做到。。），确保日志对象的生命期有效（如果业务层在全局对象的析构函数中写日志。。）。</p>

<p>三 业务线程的写入实现</p>

<p>先看一下日志的格式：</p>

<p>2017-02-21[16:06:25.764][INF]:Hello, I am listen on 8402|139872813981632</p>

<p>可以看到，日志的前半部分，是时间戳 + 日志级别，它们的长度是固定的。冒号后面是业务填写的日志内容，变长。竖线后面是线程的id（之所以线程id放最后面，因为不同系统上长度可能不同）。</p>

<p>实际上，前面框架图我画的忽略了非常多的代码细节。一条日志先要经过格式化，参数的填充，时间戳、等级的填充，最终才可以放到buffer中交给IO线程处理。图上并没有反应出来。格式化的过程使用了一个中间缓冲区，我称为tmpBuffer：</p>

<p>static const size<em>t kMaxCharPerLog = 2048;
static thread</em>local char tmpBuffer_[kMaxCharPerLog];</p>

<p>（ 这里我简单设置为单条日志不超过2KB，实际可以改为参数控制，不是重点，先忽略）</p>

<p>可能有人会问为什么要定义成线程局部变量，直接用局部变量不好吗？答案是：为了优化。看下日志的时间戳格式化，一般来说，秒之前的部分变化极少，因此很有必要将格式化的结果缓存起来，让日志之间复用这段buffer。同样我也使用了一个线程局部的缓冲区缓存线程id的字符串，线程id是不变的，更适合缓存：static thread<em>local char tid</em>[16]。之所以static，是因为可以供所有的logger对象共享这些内容，假如一个业务线程操作多个logger对象的情况。最后说下，缓存时间的做法来自陈硕的blog，最初我有点无视这点性能提升，实测之后发现比我想象的要好，于是采用了。</p>

<p>当tmpBuffer中将日志格式化好之后，才放入buffer，也就是图上的buffer，在代码中定义是BufferInfo类型。BufferInfo是thread<em>local变量，这样每个业务线程有自己的日志BufferInfo，不会冲突。而且BufferInfo要保证线程互斥访问，因为IO线程要读它。ananas::logger采用了map来管理这些BufferInfo，使用thread</em>id作索引。并使用锁来保护，因为无论是map还是BufferInfo本身都不是线程安全的：</p>

<p>struct BufferInfo
{
       std::atomic<bool> inuse<em>{false};
       Buffer buffer</em>;
};</bool></p>

<p>std::mutex mutex<em>;
std::map<std::thread::id, std::unique_ptr<bufferinfo=""> &gt; buffers</std::thread::id,></em>;</p>

<p>业务线程这样来获取BufferInfo，详见代码注释：</p>

<pre><code>BufferInfo* info = nullptr;
{   
    std::unique_lock&lt;std::mutex&gt; guard(mutex_);
    if (shutdown_)
    {   

        //不应该发生，除非是全局对象析构函数中写日志，此时日志IO线程已退出
        std::cout &lt;&lt; tmpBuffer_;
        return;
    }   

    info = buffers_[std::this_thread::get_id()].get();
    if (!info) // 没找到自己的buffer就新建一个
        buffers_[std::this_thread::get_id()].reset(info = new BufferInfo());


    // inuse_标志使得此buffer在使用过程中不会被IO线程访问。
    assert (!info-&gt;inuse_);
    info-&gt;inuse_ = true;
}   

// 好啦，锁释放了，在使用锁的过程中没有任何耗时操作。
</code></pre>

<p>在获取到BufferInfo之后，业务线程写入logger等级和内容长度信息，代码就不展示了。正常情况下写日志就结束了，但是，如果生产者过快，缓存的日志内容很多，就需要主动唤醒IO线程：</p>

<p>if (info-&gt;buffer_.ReadableSize() &gt; kFlushThreshold)
  { </p>

<pre><code>  // 日志内容太多，AddBusyLog实际上会唤醒IO线程写日志
  info-&gt;inuse_ = false;
  LogManager::Instance().AddBusyLog(this);
</code></pre>

<p>}
  else
  {
       info-&gt;inuse_ = false;
  }</p>

<p>写日志的过程到此就结束了，下面看看IO线程如何将日志写入到文件。</p>

<p>四 IO线程的日志落地实现</p>

<p>IO线程所做的事很简单，就是在BufferInfo集的条件变量上定时等待，默认设置的等待时间是5ms，我觉得3-10ms都是OK的。但如果日志过多，它会被业务线程提前唤醒，这是与sleep的本质不同。线程主体代码如下：</p>

<pre><code>bool run = true;
while (run)
{
    std::vector&lt;Logger* &gt; tmpBusy;

    {
        std::unique_lock&lt;std::mutex&gt; guard(mutex_);
        cond_.wait_for(guard, kFlushInterval); // 最多等待5ms
        if (!busyLogs_.empty())
        {

            //说明是被唤醒，有业务线程调用了AddBusyLog
            tmpBusy.assign(busyLogs_.begin(), busyLogs_.end());
            busyLogs_.clear();
        }

        if (shutdown_)
            run = false;
    }

    if (tmpBusy.empty())
    {

        // 说明是超时醒来
        std::unique_lock&lt;std::mutex&gt; guard(logsMutex_);
        for (auto&amp; plog : logs_)
            tmpBusy.push_back(plog.get());
    }

    for (auto plog : tmpBusy)
    {  // 执行落地操作，Update函数具体见下面
        plog-&gt;Update();
    }
}
</code></pre>

<p>单个logger对象的Update落地函数实现（代码中assert比较多，这不但验证了代码的健壮性，也增加了可读性），其中的sth_todo是为了告知IO线程，我还有数据需要落地，你不要急着退出。</p>

<p>bool  sth_todo = false;</p>

<p>{
    std::unique<em>lock<std::mutex> guard(mutex</std::mutex></em>);</p>

<pre><code>for (auto it(buffers_.begin()); it != buffers_.end(); )
</code></pre>

<p>{ <br>
        assert (it-&gt;second);</p>

<pre><code>    if (it-&gt;second-&gt;inuse_)
    {   
         // if logs is still in producing, there will be some work to do.
         sth_todo = true;
        ++ it; 
    }   
    else
    {   
           tmpBufs.push_back(std::move(it-&gt;second));
           it = buffers_.erase(it);
    }   
</code></pre>

<p>} <br>
}</p>

<p>// 执行落地操作
for (auto&amp; pbuf : tmpBufs)
{ 
    auto nWritten = <em>Log(pbuf-&gt;buffer</em>.ReadAddr(), pbuf-&gt;buffer<em>.ReadableSize());
    assert (nWritten == pbuf-&gt;buffer</em>.ReadableSize());
} 
// 每次写了文件都要flush
file_.flush();</p>

<p>还有一些关于流式输出的实现，我觉得都是C++语法运用的问题了，不是什么重点，大家参考下protobuf的日志的流式实现就行了。以及未开启日志级别时不评估计算参数，这是流式天然具备的，printf流的担心是没必要的。</p>

<p>五 性能思考</p>

<p>在公司的tlinux2.0系统开发机上和我的2013rMBP上做了测试，O2优化：</p>

<pre><code>1业务线程： 210万行/秒

2业务线程：310万行/秒

4业务线程：350万行/秒

8业务线程：320万行/秒
</code></pre>

<p>性能比陈硕的muduo logger高出50%（他的书中声称是200万行/秒）。而我的实现原理更为简单，没有什么双缓冲、环形buffer这些东东。主要得益于对临界区的精简，没有在临界区做任何耗时的事情。所以没那么多玄学，不是代码优化的好，而是我没有做劣化的操作。</p>

<p>最后补充个测试细节，在两个平台上，使用mmap映射日志文件，性能都是300w左右；使用std::fstream，在linux上仍然是300w，但在mbp上使用flush性能降的厉害，因此最后我提供了mmap的支持。当然如果只在linux上跑使用标准库fstream就行了。</p>
<!-- content end -->
</div>
<br>
<br>
    <div id="disqus_thread"></div>
	<div class="footer">
		<p>© Copyright 2014 by isnowfy, Designed by isnowfy</p>
	</div>
</div>
<script src="main.js"></script>
<script id="content" type="text/mustache">
    <h1>{{title}}</h1>
    <div class="tag">
    {{date}}
    {{#tags}}
    <a href="/#/tag/{{name}}">#{{name}}</a>
    {{/tags}}
    </div>
</script>
<script id="pagesTemplate" type="text/mustache">
    {{#pages}}
    <li>
        <a href="{{path}}">{{title}}</a>
    </li>
    {{/pages}}
</script>
<script>
$(document).ready(function() {
    $.ajax({
        url: "main.json",
        type: "GET",
        dataType: "json",
        success: function(data) {
            $("#title").html(data.name);
            var pagesTemplate = Hogan.compile($("#pagesTemplate").html());
            var pagesHtml = pagesTemplate.render({"pages": data.pages});
            $("#pages").append(pagesHtml);
            //path
            var path = "ananas-logger";
            //path end
            var now = 0;
            for (var i = 0; i < data.posts.length; ++i)
                if (path == data.posts[i].path)
                    now = i;
            var post = data.posts[now];
            var tmp = post.tags.split(" ");
            var tags = [];
            for (var i = 0; i < tmp.length; ++i)
                if (tmp[i].length > 0)
                    tags.push({"name": tmp[i]});
            var contentTemplate = Hogan.compile($("#content").html());
            var contentHtml = contentTemplate.render({"title": post.title, "tags": tags, "date": post.date});
            $("#main").prepend(contentHtml);
            if (data.disqus_shortname.length > 0) {
                var disqus_shortname = data.disqus_shortname;
                (function() {
                    var dsq = document.createElement('script'); dsq.type = 'text/javascript'; dsq.async = true;
                    dsq.src = '//' + disqus_shortname + '.disqus.com/embed.js';
                    (document.getElementsByTagName('head')[0] || document.getElementsByTagName('body')[0]).appendChild(dsq);
                })();
            }
        }
    });
});
</script>
<script src="http://cdn.mathjax.org/mathjax/latest/MathJax.js?config=TeX-AMS-MML_HTMLorMML"></script>
<script type="text/x-mathjax-config">
    MathJax.Hub.Config({tex2jax: {inlineMath: [['$','$'], ["\\(", "\\)"]], processEscapes: true}});
</script>
</body>
</html>
